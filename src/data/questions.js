export const DATA = [
  /* ====== 第 1 個月：JS / TS 基礎 ====== */
  { m:1,w:1,d:1,t:"觀念題",q:"map 和 forEach 有什麼差異？什麼情況該用哪一個？",a:"<b>map</b> 會回傳一個<b>新陣列</b>，適合「把資料轉成另一種形狀」，例如把 API 資料轉成畫面要的格式。<b>forEach</b> 沒有回傳值，只是逐一執行動作，適合純粹的副作用（如 console、累加到外部變數）。後台開發中 90% 的資料轉換都該用 map / filter / reduce，因為可以串接且不改動原始資料。" },
  { m:1,w:1,d:2,t:"程式題",q:"有商品陣列 products = [{name:'A', price:100, stock:0}, {name:'B', price:250, stock:3}, {name:'C', price:80, stock:5}]。請用 filter 和 map 取出「有庫存的商品名稱」陣列。",a:"<pre>const names = products\n  .filter(p => p.stock > 0)\n  .map(p => p.name);\n// ['B', 'C']</pre>先 filter 篩出符合條件的物件，再 map 轉成需要的欄位。這個「先篩選、再轉換」的組合是後台表格資料處理最常見的模式。" },
  { m:1,w:1,d:3,t:"程式題",q:"延續昨天的 products，請用 reduce 計算「有庫存商品的總價值」（price × stock 加總）。",a:"<pre>const total = products\n  .filter(p => p.stock > 0)\n  .reduce((sum, p) => sum + p.price * p.stock, 0);\n// 250*3 + 80*5 = 1150</pre><b>reduce 重點：</b>第二個參數 <code>0</code> 是初始值，一定要給；callback 的第一個參數是累加器，每一輪要 return 出去，下一輪才接得到。" },
  { m:1,w:1,d:4,t:"程式題",q:"API 回傳 res = { data: { list: [...], total: 20 } }，但有時候 data 可能是 null。請用解構 + 預設值安全地取出 list 和 total。",a:"<pre>const { list = [], total = 0 } = res.data ?? {};</pre>兩層防護：<code>??</code> 在 data 是 null / undefined 時退回空物件，解構的 <code>= []</code> 和 <code>= 0</code> 在屬性不存在時給預設值。這樣畫面渲染 <code>list.map(...)</code> 就不會炸掉。" },
  { m:1,w:1,d:5,t:"觀念題",q:"展開運算子 {...obj} 是深拷貝還是淺拷貝？如果物件裡還有巢狀物件，要怎麼真正複製一份？",a:"<code>{...obj}</code> 是<b>淺拷貝</b>：第一層是新的，但巢狀物件仍指向同一個記憶體位置，改了會互相影響。需要深拷貝時：<pre>const copy = structuredClone(obj); // 現代瀏覽器內建\n// 舊方法：JSON.parse(JSON.stringify(obj))\n// 注意 JSON 法會丟失 undefined、function、Date 會變字串</pre>面試常考：表單編輯 Modal 帶入資料時若用淺拷貝，可能改到列表的原始資料。" },
  { m:1,w:2,d:1,t:"觀念題",q:"Promise 有哪三種狀態？狀態之間怎麼轉換？",a:"<ul><li><b>pending</b>（進行中）：初始狀態</li><li><b>fulfilled</b>（成功）：呼叫 resolve 後</li><li><b>rejected</b>（失敗）：呼叫 reject 或拋出錯誤後</li></ul>狀態<b>只能轉換一次</b>且不可逆：pending → fulfilled 或 pending → rejected。fulfilled 走 <code>.then</code>，rejected 走 <code>.catch</code>。" },
  { m:1,w:2,d:2,t:"程式題",q:"把這段 .then 寫法改成 async / await：<br><code>fetch('/api/users').then(r => r.json()).then(data => console.log(data))</code>",a:"<pre>async function loadUsers() {\n  const res = await fetch('/api/users');\n  const data = await res.json();\n  console.log(data);\n}</pre><b>兩個重點：</b>await 只能在 async function 裡用；<code>res.json()</code> 本身也回傳 Promise，所以也要 await。" },
  { m:1,w:2,d:3,t:"程式題",q:"幫昨天的 loadUsers 加上完整的錯誤處理：API 失敗（HTTP 4xx/5xx）和網路錯誤都要能接到。",a:"<pre>async function loadUsers() {\n  try {\n    const res = await fetch('/api/users');\n    if (!res.ok) {\n      throw new Error('HTTP ' + res.status);\n    }\n    return await res.json();\n  } catch (err) {\n    console.error('載入失敗:', err.message);\n    throw err;\n  }\n}</pre><b>關鍵：</b>fetch 收到 404/500 <b>不會</b>自動 reject，要自己檢查 <code>res.ok</code>。" },
  { m:1,w:2,d:4,t:"程式題",q:"頁面載入時要同時打「使用者資料」和「訂單列表」兩支互不相依的 API。怎麼寫最有效率？",a:"<pre>const [user, orders] = await Promise.all([\n  fetchUser(),\n  fetchOrders(),\n]);</pre>用 <b>Promise.all</b> 並行發出，總時間 = 最慢的那支，而不是兩支相加。注意：其中一支失敗整個會 reject；若希望「各自成敗互不影響」改用 <code>Promise.allSettled</code>。" },
  { m:1,w:2,d:5,t:"觀念題",q:"以下程式的輸出順序是什麼？為什麼？<pre>console.log('1');\nsetTimeout(() => console.log('2'), 0);\nPromise.resolve().then(() => console.log('3'));\nconsole.log('4');</pre>",a:"<b>輸出：1 → 4 → 3 → 2</b><ul><li>同步程式先跑完：1、4</li><li>Promise.then 進<b>微任務佇列</b>，同步結束後立刻執行：3</li><li>setTimeout 進<b>宏任務佇列</b>，要等微任務清空才輪到：2</li></ul>event loop 基本順序：同步 → 微任務 → 宏任務。" },
  { m:1,w:3,d:1,t:"觀念題",q:"TypeScript 的 type 和 interface 有什麼差異？實務上怎麼選？",a:"<ul><li><b>interface</b>：描述物件形狀，可以 extends 繼承、同名會自動合併</li><li><b>type</b>：更萬用，可以定義 union、交集、原始型別別名、函式型別</li></ul><b>實務選法：</b>物件結構用 interface 或 type 都可以，團隊統一就好；需要 union、組合型別時只能用 type。" },
  { m:1,w:3,d:2,t:"程式題",q:"用 union type 定義一個 API 請求狀態 Status，只允許 'idle'、'loading'、'success'、'error' 四種值，並寫一個變數使用它。",a:"<pre>type Status = 'idle' | 'loading' | 'success' | 'error';\n\nlet status: Status = 'idle';\nstatus = 'loading'; // OK\nstatus = 'done';    // ❌ 編譯錯誤</pre>這叫<b>字面值聯合型別</b>，比用 string 安全：打錯字、漏狀態編譯期就會抓到。" },
  { m:1,w:3,d:3,t:"程式題",q:"定義一個 User interface：id（數字）、name（字串）、email（字串）、phone（可有可無）。並示範安全地印出 phone 的長度。",a:"<pre>interface User {\n  id: number;\n  name: string;\n  email: string;\n  phone?: string;\n}\n\nconst u: User = { id: 1, name: 'Amy', email: 'a@x.com' };\nconsole.log(u.phone?.length ?? 0);</pre><code>?</code> 宣告可選屬性；存取時用<b>可選串連</b> <code>?.</code> 避免 undefined 報錯，再用 <code>??</code> 給預設值。" },
  { m:1,w:3,d:4,t:"程式題",q:"後端分頁查詢 API 回傳 { code: 0, message: 'ok', data: { list: [...使用者], total: 100, page: 1 } }。請設計完整的 response 型別。",a:"<pre>interface User {\n  id: number;\n  name: string;\n  email: string;\n}\ninterface PageData {\n  list: User[];\n  total: number;\n  page: number;\n}\ninterface UserListResponse {\n  code: number;\n  message: string;\n  data: PageData;\n}</pre><b>設計原則：</b>由內往外拆，每一層一個 interface，欄位名稱完全照後端 response 對。" },
  { m:1,w:3,d:5,t:"程式題",q:"昨天的 code / message / data 結構其實每支 API 都一樣，只有 data 不同。請用 generic 改寫成可重用的 ApiResponse&lt;T&gt;。",a:"<pre>interface ApiResponse&lt;T&gt; {\n  code: number;\n  message: string;\n  data: T;\n}\n\ntype UserListRes = ApiResponse&lt;PageData&gt;;\ntype UserDetailRes = ApiResponse&lt;User&gt;;\ntype DeleteRes = ApiResponse&lt;null&gt;;</pre><b>generic 的本質：</b>把「會變動的部分」變成型別參數 T。" },
  { m:1,w:4,d:1,t:"程式題",q:"把這支 JS 函式加上完整型別（參數與回傳值）：<pre>async function getUser(id) {\n  const res = await fetch('/api/users/' + id);\n  return res.json();\n}</pre>",a:"<pre>async function getUser(id: number): Promise&lt;ApiResponse&lt;User&gt;&gt; {\n  const res = await fetch('/api/users/' + id);\n  if (!res.ok) throw new Error('HTTP ' + res.status);\n  return res.json() as Promise&lt;ApiResponse&lt;User&gt;&gt;;\n}</pre>async 函式的回傳型別一定是 <code>Promise&lt;...&gt;</code>。" },
  { m:1,w:4,d:2,t:"程式題",q:"設計一個「新增使用者」表單的型別 UserForm（name、email、role 限定 'admin' | 'user'、備註可選），並寫出表單初始值。",a:"<pre>type Role = 'admin' | 'user';\ninterface UserForm {\n  name: string;\n  email: string;\n  role: Role;\n  note?: string;\n}\nconst initialForm: UserForm = {\n  name: '',\n  email: '',\n  role: 'user',\n};</pre>" },
  { m:1,w:4,d:3,t:"程式題",q:"定義一個 React 按鈕元件的 props 型別：text（字串）、onClick（點擊函式）、disabled（可選布林）、children 不需要。",a:"<pre>interface ButtonProps {\n  text: string;\n  onClick: () => void;\n  disabled?: boolean;\n}\nfunction MyButton({ text, onClick, disabled = false }: ButtonProps) {\n  return (\n    &lt;button onClick={onClick} disabled={disabled}&gt;{text}&lt;/button&gt;\n  );\n}</pre>" },
  { m:1,w:4,d:4,t:"程式題",q:"API 回傳的使用者資料中 lastLoginAt 可能是字串也可能是 null。請設計型別，並寫出畫面顯示邏輯（null 時顯示「從未登入」）。",a:"<pre>interface User {\n  id: number;\n  name: string;\n  lastLoginAt: string | null;\n}\nconst text = user.lastLoginAt ?? '從未登入';</pre><b>原則：</b>後端說「可能沒有值」的欄位，型別就誠實寫 <code>| null</code>，逼自己在畫面層處理空值。" },
  { m:1,w:4,d:5,t:"任務題",q:"【本週任務】完成一個 TypeScript API 串接小練習：用任一免費 API（如 jsonplaceholder.typicode.com/users）做出「載入並顯示使用者列表」。",a:"<b>驗收清單：</b><ul><li>☐ 為 API response 定義了 interface（不是 any）</li><li>☐ 有一支獨立的 getUsers() 函式負責呼叫 API</li><li>☐ 有 try/catch 與 res.ok 檢查</li><li>☐ 畫面能顯示 loading 中、錯誤、與資料列表三種狀態</li><li>☐ 程式碼放上 GitHub，附簡短 README</li></ul>" },

  /* ====== 第 2 個月：React 主線 ====== */
  { m:2,w:1,d:1,t:"觀念題",q:"props 和 state 的差異是什麼？哪個可以被元件自己修改？",a:"<ul><li><b>props</b>：由父元件傳入，對子元件是<b>唯讀</b>的，子元件不能直接改</li><li><b>state</b>：元件自己擁有的資料，透過 setState 修改，改變會觸發重新渲染</li></ul>判斷原則：資料是「別人給我的」就是 props，「我自己管理、會隨互動改變的」就是 state。" },
  { m:2,w:1,d:2,t:"程式題",q:"寫一個 StatusTag 元件：接收 props status（'active' | 'inactive'），active 顯示綠色「啟用」，inactive 顯示灰色「停用」。",a:"<pre>interface StatusTagProps {\n  status: 'active' | 'inactive';\n}\nfunction StatusTag({ status }: StatusTagProps) {\n  const isActive = status === 'active';\n  return (\n    &lt;span style={{ color: isActive ? 'green' : 'gray' }}&gt;\n      {isActive ? '啟用' : '停用'}\n    &lt;/span&gt;\n  );\n}</pre>" },
  { m:2,w:1,d:3,t:"觀念題",q:"state 是一個物件 form = { name: '', email: '' }。為什麼不能寫 form.name = 'Amy' 來更新？正確寫法是什麼？",a:"React 用 <b>Object.is 比較參考</b>來判斷 state 有沒有變。直接改屬性，物件參考沒變，React 不會重新渲染。正確做法是建立<b>新物件</b>：<pre>setForm({ ...form, name: 'Amy' });\n// 或函式式更新\nsetForm(prev => ({ ...prev, name: 'Amy' }));</pre>" },
  { m:2,w:1,d:4,t:"程式題",q:"做一個受控元件：input 輸入文字，下方即時顯示「你好，{輸入的名字}」。",a:"<pre>function Greeting() {\n  const [name, setName] = useState('');\n  return (\n    &lt;div&gt;\n      &lt;input\n        value={name}\n        onChange={(e) =&gt; setName(e.target.value)}\n      /&gt;\n      &lt;p&gt;你好，{name}&lt;/p&gt;\n    &lt;/div&gt;\n  );\n}</pre><b>受控元件</b> = input 的值由 state 控制（value + onChange 成對出現）。" },
  { m:2,w:1,d:5,t:"觀念題",q:"列表渲染時為什麼要給 key？為什麼不建議用 index 當 key？",a:"key 幫助 React 在重新渲染時<b>辨認哪些項目是同一個</b>，做最小幅度更新。用 index 當 key 的問題：當列表<b>排序、刪除、插入</b>時，index 會對不上原本的項目，導致畫面錯亂。<b>正確做法：</b>用資料中穩定唯一的值，通常是後端給的 id。" },
  { m:2,w:2,d:1,t:"觀念題",q:"useEffect 的依賴陣列有三種寫法：不寫、[]、[a, b]。各代表什麼時候執行？",a:"<ul><li><b>不寫</b>：每次渲染後都執行</li><li><b>[]（空陣列）</b>：只在元件掛載後執行一次，常用於初始載入資料</li><li><b>[a, b]</b>：掛載後執行一次 + a 或 b 改變時再執行</li></ul>原則：effect 裡用到的外部變數，都應該誠實放進依賴陣列。" },
  { m:2,w:2,d:2,t:"程式題",q:"寫一個元件：掛載時呼叫 getUsers() 載入列表，載入中顯示「載入中…」，完成後顯示人數。",a:"<pre>function UserCount() {\n  const [users, setUsers] = useState([]);\n  const [loading, setLoading] = useState(true);\n\n  useEffect(() =&gt; {\n    getUsers()\n      .then(res =&gt; setUsers(res.data.list))\n      .finally(() =&gt; setLoading(false));\n  }, []);\n\n  if (loading) return &lt;p&gt;載入中…&lt;/p&gt;;\n  return &lt;p&gt;共 {users.length} 位使用者&lt;/p&gt;;\n}</pre>" },
  { m:2,w:2,d:3,t:"觀念題",q:"useEffect 的 cleanup（return 的函式）是做什麼用的？舉兩個實際使用場景。",a:"cleanup 在<b>元件卸載前</b>以及<b>下一次 effect 執行前</b>被呼叫，用來收拾上一輪留下的東西。<b>場景：</b><ul><li>清除計時器：<code>return () =&gt; clearInterval(timer)</code></li><li>取消事件監聽：<code>return () =&gt; window.removeEventListener(...)</code></li><li>中止過期的 API 請求（AbortController），避免 race condition</li></ul>" },
  { m:2,w:2,d:4,t:"觀念題",q:"這段程式為什麼會造成無限迴圈？怎麼修？<pre>const [data, setData] = useState([]);\nuseEffect(() => {\n  fetchData().then(setData);\n});</pre>",a:"<b>原因：</b>沒寫依賴陣列 → 每次渲染後都執行 effect → setData 觸發重新渲染 → 又執行 effect → 無限循環。<b>修法：</b>加上空依賴陣列：<pre>useEffect(() =&gt; {\n  fetchData().then(setData);\n}, []); // 只在掛載時執行一次</pre>" },
  { m:2,w:2,d:5,t:"程式題",q:"完成一個列表元件的三狀態渲染：loading 顯示「載入中」、error 顯示錯誤訊息與重試按鈕、資料為空顯示「目前沒有資料」。",a:"<pre>if (loading) return &lt;p&gt;載入中…&lt;/p&gt;;\n\nif (error) return (\n  &lt;div&gt;\n    &lt;p&gt;載入失敗：{error}&lt;/p&gt;\n    &lt;button onClick={reload}&gt;重試&lt;/button&gt;\n  &lt;/div&gt;\n);\n\nif (list.length === 0) return &lt;p&gt;目前沒有資料&lt;/p&gt;;\n\nreturn &lt;Table rows={list} /&gt;;</pre><b>順序很重要：</b>loading → error → empty → 正常資料，用 early return 一層層擋。" },
  { m:2,w:3,d:1,t:"程式題",q:"用 React Router 設定兩個頁面：/users 顯示 UserList、/users/:id 顯示 UserDetail。",a:"<pre>import { BrowserRouter, Routes, Route } from 'react-router-dom';\n\nfunction App() {\n  return (\n    &lt;BrowserRouter&gt;\n      &lt;Routes&gt;\n        &lt;Route path=\"/users\" element={&lt;UserList /&gt;} /&gt;\n        &lt;Route path=\"/users/:id\" element={&lt;UserDetail /&gt;} /&gt;\n      &lt;/Routes&gt;\n    &lt;/BrowserRouter&gt;\n  );\n}</pre><code>:id</code> 是動態參數，在 UserDetail 裡用 <code>useParams()</code> 取得。" },
  { m:2,w:3,d:2,t:"觀念題",q:"useNavigate 和 useParams 各是做什麼的？列表頁點某一列跳到詳細頁，兩個怎麼配合？",a:"<ul><li><b>useNavigate</b>：用程式控制跳轉，<code>navigate('/users/' + row.id)</code></li><li><b>useParams</b>：在目標頁取得網址上的動態參數，<code>const { id } = useParams()</code></li></ul>流程：列表頁點擊 → navigate 帶 id 跳轉 → 詳細頁 useParams 拿到 id → useEffect 用 id 打單筆 API → 渲染。" },
  { m:2,w:3,d:3,t:"程式題",q:"一個查詢表單有 keyword、status、startDate 三個欄位。請用「一個物件 state」管理它們，並寫出通用的 onChange 處理。",a:"<pre>const [query, setQuery] = useState({\n  keyword: '', status: 'all', startDate: '',\n});\n\nconst handleChange = (field, value) =&gt; {\n  setQuery(prev =&gt; ({ ...prev, [field]: value }));\n};\n\n// 使用\n&lt;input value={query.keyword}\n  onChange={e =&gt; handleChange('keyword', e.target.value)} /&gt;</pre>" },
  { m:2,w:3,d:4,t:"觀念題",q:"表單送出時為什麼要呼叫 e.preventDefault()？不寫會發生什麼事？",a:"HTML form 的預設行為是<b>提交並重新載入整個頁面</b>。SPA 裡我們要自己用 JS 打 API，所以必須 <code>e.preventDefault()</code> 取消預設行為，否則頁面一閃重整、state 全部歸零、API 還沒打完就被中斷。<pre>const onSubmit = (e) =&gt; {\n  e.preventDefault();\n  search(query);\n};</pre>" },
  { m:2,w:3,d:5,t:"程式題",q:"設計「新增 / 編輯共用一個 Modal」的 state：要能知道 Modal 開或關、目前是新增還是編輯、編輯的是哪一筆。",a:"<pre>const [modal, setModal] = useState({ open: false });\n\n// 開新增\nsetModal({ open: true, mode: 'create' });\n// 開編輯\nsetModal({ open: true, mode: 'edit', row });\n// 關閉\nsetModal({ open: false });</pre>用 union type 把「不可能的狀態」從型別上排除，比三個零散的 useState 不容易出 bug。" },
  { m:2,w:4,d:1,t:"觀念題",q:"一個後台查詢頁（查詢條件 + 表格 + 分頁 + 編輯 Modal）應該怎麼拆元件？每個元件的責任是什麼？",a:"<pre>UserPage（容器：擁有 query / list / modal 等 state，負責打 API）\n├── SearchForm（收集查詢條件，送出時呼叫 props.onSearch）\n├── UserTable（純顯示資料，點編輯時呼叫 props.onEdit(row)）\n├── Pagination（顯示頁碼，換頁呼叫 props.onPageChange）\n└── UserModal（表單本身，送出呼叫 props.onSubmit）</pre><b>原則：</b>state 與 API 集中在頁面層，子元件只透過 props 收資料、發事件。" },
  { m:2,w:4,d:2,t:"程式題",q:"設計分頁的 state 與換頁邏輯：page、pageSize、total，並算出總頁數。",a:"<pre>const [page, setPage] = useState(1);\nconst [pageSize] = useState(10);\nconst [total, setTotal] = useState(0);\n\nconst totalPages = Math.ceil(total / pageSize);\n\nuseEffect(() =&gt; {\n  fetchList({ page, pageSize, ...query }).then(res =&gt; {\n    setList(res.data.list);\n    setTotal(res.data.total);\n  });\n}, [page]);</pre>" },
  { m:2,w:4,d:3,t:"觀念題",q:"查詢頁的「清除」按鈕按下去，應該發生哪些事？常見的遺漏是什麼？",a:"<b>完整行為：</b><ol style='margin-left:22px'><li>查詢條件 state 重設回初始值</li><li>page 重設回第 1 頁</li><li>用初始條件<b>重新查詢一次</b></li></ol><b>常見遺漏：</b>只清了 input 畫面，但實際送出的 query state 沒清；或忘記重設 page，導致空條件查詢卻停在第 5 頁看到空白。" },
  { m:2,w:4,d:4,t:"觀念題",q:"useMemo 和 useCallback 各解決什麼問題？什麼時候「不需要」用？",a:"<ul><li><b>useMemo</b>：快取「計算結果」，依賴沒變就不重算。用於昂貴計算（大量資料 filter / sort）</li><li><b>useCallback</b>：快取「函式參考」，依賴沒變函式就是同一個。用於把函式傳給被 React.memo 包住的子元件</li></ul><b>不需要用：</b>計算很便宜、子元件沒有 memo 化時，加了只是徒增複雜度。" },
  { m:2,w:4,d:5,t:"任務題",q:"【本週任務】完成 React 後台查詢頁 Demo。",a:"<b>驗收清單（對照計畫第 2 個月產出）：</b><ul><li>☐ 查詢條件 + 查詢按鈕 + 清除按鈕</li><li>☐ 表格列表（MUI DataGrid 或一般 table）</li><li>☐ 分頁功能</li><li>☐ 新增按鈕 + 編輯 Modal</li><li>☐ loading / error / empty 三狀態</li><li>☐ 元件至少拆成 SearchForm / Table / Modal</li><li>☐ 能口頭說明每個 component 的責任</li></ul>" },

  /* ====== 第 3 個月：API 與資料流 ====== */
  { m:3,w:1,d:1,t:"觀念題",q:"RESTful API 中 GET / POST / PUT / DELETE 分別對應 CRUD 的哪個動作？以 /users 資源各舉一例。",a:"<ul><li><b>GET /users</b> → Read 列表；<b>GET /users/1</b> → Read 單筆</li><li><b>POST /users</b> → Create 新增（資料放 body）</li><li><b>PUT /users/1</b> → Update 更新整筆（PATCH 為部分更新）</li><li><b>DELETE /users/1</b> → Delete 刪除</li></ul>規律：<b>網址描述資源、動詞描述操作</b>。" },
  { m:3,w:1,d:2,t:"觀念題",q:"query params 和 request body 有什麼差別？查詢條件和新增表單的資料分別該放哪裡？",a:"<ul><li><b>query params</b>：接在網址後 <code>?keyword=amy&page=1</code>，用於 GET 的<b>查詢 / 篩選 / 分頁</b>條件</li><li><b>request body</b>：放在請求本體，用於 POST / PUT 要<b>建立或修改的資料</b></li></ul>所以：查詢條件 → query params；新增表單 → body。" },
  { m:3,w:1,d:3,t:"觀念題",q:"HTTP 狀態碼 200、400、401、403、404、500 各代表什麼？前端各該怎麼反應？",a:"<ul><li><b>200</b> 成功 → 正常渲染</li><li><b>400</b> 請求參數錯誤 → 顯示後端錯誤訊息</li><li><b>401</b> 未登入 / token 過期 → 導回登入頁</li><li><b>403</b> 沒有權限 → 顯示無權限提示</li><li><b>404</b> 資源不存在 → 顯示查無資料</li><li><b>500</b> 伺服器錯誤 → 顯示通用錯誤、提供重試</li></ul>" },
  { m:3,w:1,d:4,t:"觀念題",q:"為什麼不建議在 component 裡直接寫 fetch，而要拆出一層 API service？",a:"<b>API service 分層的好處：</b><ul><li>共用邏輯集中：baseURL、token header、錯誤處理、response 解包只寫一次</li><li>元件變乾淨：元件只關心「呼叫 getUsers() 拿到資料」，不關心 HTTP 細節</li><li>好替換好測試：換 Mock API、換 axios、寫測試都只動 service 層</li></ul>" },
  { m:3,w:1,d:5,t:"程式題",q:"用文字寫出一條完整資料流：使用者在查詢頁按下「查詢」按鈕後，到資料出現在表格上，中間經過哪些步驟？",a:"<pre>1. 按鈕 onClick 觸發 handleSearch\n2. handleSearch 整理 query state → 呼叫 api/user.ts 的 getUsers(params)\n3. service 組出 GET /api/users?keyword=...&page=1 發出請求\n4. 後端回傳 response JSON { code, data: { list, total } }\n5. service 檢查狀態、解包出 data\n6. 頁面 setList(data.list)、setTotal(data.total)\n7. state 改變 → React 重新渲染 → Table 收到新 props → 畫面更新</pre>" },
  { m:3,w:2,d:1,t:"觀念題",q:"分頁 API 的參數通常怎麼設計？前端送什麼、後端回什麼？換 pageSize 時前端要注意什麼？",a:"<b>前端送：</b><code>page</code>、<code>pageSize</code>（也有用 offset / limit 的）。<b>後端回：</b><code>list</code> 當頁資料 + <code>total</code> 總筆數。<b>注意：</b>pageSize 改變時要把 page 重設回 1；串接前先和後端確認 page 從 0 還是 1 開始。" },
  { m:3,w:2,d:2,t:"程式題",q:"寫一支查詢 API service：getUsers({ keyword, page, pageSize })，把參數轉成 query string 發出 GET。",a:"<pre>export async function getUsers(q) {\n  const params = new URLSearchParams();\n  if (q.keyword) params.set('keyword', q.keyword);\n  params.set('page', String(q.page));\n  params.set('pageSize', String(q.pageSize));\n\n  const res = await fetch('/api/users?' + params.toString());\n  if (!res.ok) throw new Error('HTTP ' + res.status);\n  return res.json();\n}</pre>用 <code>URLSearchParams</code> 組字串可自動處理編碼。" },
  { m:3,w:2,d:3,t:"觀念題",q:"使用者輸入關鍵字查詢後翻到第 3 頁，再按「下一頁」。要怎麼確保查詢條件不會掉？常見的 bug 寫法是什麼？",a:"<b>正確設計：</b>查詢條件存在 state，<b>每次</b>呼叫列表 API 都帶上「目前條件 + 目前頁碼」。<b>常見 bug：</b>查詢按鈕用的是表單值、換頁卻只送 page 不帶條件，於是翻頁後條件消失、列表變回全部資料。" },
  { m:3,w:2,d:4,t:"程式題",q:"詳細頁需要打 GET /api/users/:id。寫出 service 與頁面端取得 id 並載入的程式。",a:"<pre>// api/user.ts\nexport async function getUser(id) {\n  const res = await fetch('/api/users/' + id);\n  if (!res.ok) throw new Error('HTTP ' + res.status);\n  return res.json();\n}\n\n// 頁面元件\nconst { id } = useParams();\nuseEffect(() =&gt; {\n  if (!id) return;\n  getUser(id).then(res =&gt; setUser(res.data));\n}, [id]);</pre>" },
  { m:3,w:2,d:5,t:"程式題",q:"用 TanStack Query 的 useQuery 改寫「載入使用者列表」，並說出它比手寫 useEffect 多了哪些好處。",a:"<pre>const { data, isLoading, isError, refetch } = useQuery({\n  queryKey: ['users', query, page],\n  queryFn: () =&gt; getUsers({ ...query, page, pageSize }),\n});</pre><b>好處：</b>loading / error 狀態內建、queryKey 變了自動重新請求、內建快取、refetch 一行實現「重試」按鈕。" },
  { m:3,w:3,d:1,t:"觀念題",q:"「新增資料」的完整前端流程是什麼？從打開 Modal 到列表更新，列出每一步。",a:"<pre>1. 點「新增」→ 打開 Modal（表單為初始值）\n2. 使用者填寫 → 前端驗證（必填、格式）\n3. 通過 → POST /api/users，body 帶表單資料，按鈕進入 loading\n4. 成功 → 關閉 Modal、顯示成功提示、重新呼叫列表 API\n5. 失敗 → Modal 保持開啟、顯示後端錯誤訊息</pre>" },
  { m:3,w:3,d:2,t:"觀念題",q:"編輯 Modal 要顯示既有資料，有兩種做法：直接用列表那一列的資料、或開 Modal 時打單筆 API。各適合什麼情況？",a:"<ul><li><b>用列表資料</b>：簡單、零延遲。適合列表欄位已包含全部編輯欄位時。注意要<b>複製一份</b>進表單 state</li><li><b>打單筆 API</b>：適合詳細資料欄位比列表多、或資料常被他人異動需要最新值</li></ul>實務上中大型後台常用第二種，確保編輯的是最新資料。" },
  { m:3,w:3,d:3,t:"觀念題",q:"刪除功能要注意什麼？「樂觀更新（optimistic update）」是什麼意思？",a:"<b>刪除的基本款：</b>一定要有確認對話框 → DELETE API → 成功後重查列表或從 state 移除該筆 → 失敗顯示錯誤。<b>樂觀更新：</b>不等 API 回來，先把畫面改掉，API 失敗再還原並提示。" },
  { m:3,w:3,d:4,t:"程式題",q:"每支 API 失敗都要顯示錯誤訊息、401 要導回登入頁。怎麼避免在每個呼叫處重複寫這些邏輯？",a:"在 service 層做一個<b>統一的 request 包裝</b>：<pre>async function request(url, options) {\n  const res = await fetch(url, options);\n  if (res.status === 401) {\n    location.href = '/login';\n    throw new Error('未登入');\n  }\n  const json = await res.json();\n  if (!res.ok || json.code !== 0) {\n    throw new Error(json.message || 'HTTP ' + res.status);\n  }\n  return json.data;\n}</pre>" },
  { m:3,w:3,d:5,t:"程式題",q:"表單送出前要驗證：name 必填、email 必填且格式正確。寫出驗證函式並回傳錯誤訊息物件。",a:"<pre>function validate(form) {\n  const errors = {};\n  if (!form.name.trim()) errors.name = '請輸入姓名';\n  if (!form.email.trim()) errors.email = '請輸入 Email';\n  else if (!/^\\S+@\\S+\\.\\S+$/.test(form.email)) errors.email = 'Email 格式不正確';\n  return errors;\n}\n\nconst errors = validate(form);\nif (Object.keys(errors).length > 0) { setErrors(errors); return; }</pre>" },
  { m:3,w:4,d:1,t:"觀念題",q:"畫面上有個欄位顯示的數字怪怪的。寫出你用 DevTools「從畫面反查到 API」的標準步驟。",a:"<pre>1. 確認現象：哪個欄位、什麼值、預期是什麼\n2. 開 Network 面板 → 重新觸發查詢\n3. 找到對應的 API 請求\n4. 看 Response：後端給的原始值對不對？\n   ├─ Response 就是錯的 → 看 Payload，確認前端送的參數\n   └─ Response 是對的 → 問題在前端 mapping 或顯示邏輯\n5. 記下結論：問題在「送出參數 / 後端資料 / 前端轉換」哪一段</pre>" },
  { m:3,w:4,d:2,t:"觀念題",q:"在 Chrome DevTools 的 Network 面板裡，一個請求的「送出參數」和「回傳資料」分別在哪裡看？還有哪些常用資訊？",a:"<ul><li><b>Payload</b>：GET 的 Query String Parameters、POST 的 Request Payload——這就是前端實際送出的東西</li><li><b>Response / Preview</b>：後端回傳的原始 JSON</li><li><b>Headers</b>：請求方法、狀態碼、token 等 header</li><li><b>Timing</b>：請求耗時，慢的 API 在這裡現形</li></ul>" },
  { m:3,w:4,d:3,t:"觀念題",q:"API 回傳的某欄位是 null 或缺失，畫面上有哪些處理選項？以「最後登入時間」為例說明怎麼選。",a:"<b>選項：</b><ol style='margin-left:22px'><li>顯示替代文字：「從未登入」「—」（最常用）</li><li>隱藏該欄位</li><li>顯示預設值 0 / 空陣列</li></ol><b>原則：</b>不能讓使用者看到 <code>null</code>、<code>undefined</code>、<code>NaN</code>、<code>Invalid Date</code> 字樣。" },
  { m:3,w:4,d:4,t:"程式題",q:"後端回傳 snake_case：{ user_name, created_at, is_active }，前端慣用 camelCase。寫一個 mapping 函式轉換，並說明為什麼建議在 service 層做。",a:"<pre>function toUser(dto) {\n  return {\n    userName: dto.user_name,\n    createdAt: dto.created_at,\n    isActive: dto.is_active,\n  };\n}</pre><b>在 service 層做的理由：</b>轉換只發生一次；後端改欄位名時只改 mapping 一處。" },
  { m:3,w:4,d:5,t:"任務題",q:"【本週任務】完成完整 CRUD 後台 Demo + API 追蹤筆記。",a:"<b>驗收清單：</b><ul><li>☐ 列表查詢 + 分頁 + 查詢條件保存</li><li>☐ 新增 / 編輯 / 刪除完整流程</li><li>☐ 表單驗證</li><li>☐ API loading / error 處理、統一 request 包裝</li><li>☐ 資料 mapping 在 service 層</li><li>☐ README：功能列表、資料夾結構、Mock API 說明</li><li>☐ 一份 API 追蹤筆記：每個畫面欄位 ↔ 哪支 API 哪個欄位的對照表</li></ul>" },

  /* ====== 第 4 個月：工作案例 ====== */
  { m:4,w:1,d:1,t:"任務題",q:"用案例模板的前三段（背景 / 問題 / 追查），寫下你處理過的一個前端畫面或表格 Bug。還想不起來的話，先列出三個候選事件。",a:"<b>示範格式：</b><br><b>背景：</b>內部訂單管理系統的訂單列表頁。<br><b>問題：</b>切換到第 2 頁後用狀態篩選器查詢，表格顯示「查無資料」，但資料其實存在。<br><b>追查：</b>從篩選器的 onChange → 查詢 function → Network 看到 request 同時帶了 status=shipped 和 page=2 → 發現篩選後沒有重設頁碼。<br><br><b>檢查點：</b>背景一句講清楚「系統 + 畫面 + 誰在用」；追查要有具體路徑（事件 → function → API → 參數）。" },
  { m:4,w:1,d:2,t:"觀念題",q:"案例的「追查」段落是面試官最在意的部分。一段好的追查描述應該包含哪些元素？",a:"<b>四個元素：</b><ul><li><b>起點</b>：我從哪裡開始看（畫面現象 / 觸發事件）</li><li><b>路徑</b>：畫面 → 事件 → 前端 function → API request → response → 資料來源</li><li><b>排除法</b>：我怎麼確認問題「不在」某一層</li><li><b>工具</b>：用了什麼（Network 面板、斷點、console）</li></ul>面試官想聽的是<b>你的思考順序</b>，不是結論。" },
  { m:4,w:1,d:3,t:"任務題",q:"把這句話改寫成有過程、有結果的案例語言：「我修了一個表格顯示錯誤的 bug。」",a:"<b>改寫示範：</b>「訂單列表在<b>篩選後沒有重設頁碼</b>，導致使用者誤以為查無資料。我從篩選事件追到查詢參數，發現 page 沒有歸 1，<b>修正後並補上『條件變更即重設頁碼』的共用邏輯</b>，同類問題在其他三個列表頁也一併解決。」<br><b>公式：</b>現象 → 原因 → 修正 → 擴散價值。" },
  { m:4,w:1,d:4,t:"任務題",q:"為你 W1 的 Bug 案例補上「驗證」段：列出至少 3 個你會測的情境，涵蓋正常、邊界、回歸。",a:"<b>以「篩選未重設頁碼」為例：</b><ul><li><b>正常</b>：第 1 頁直接篩選，結果正確</li><li><b>重現路徑</b>：翻到第 2 頁再篩選，確認已修復</li><li><b>邊界</b>：篩選結果剛好 0 筆 / 剛好 1 頁滿 / 超過 1 頁</li><li><b>回歸</b>：清除按鈕、換 pageSize、重新整理後行為仍正常</li></ul>" },
  { m:4,w:1,d:5,t:"觀念題",q:"案例最後的「影響」段怎麼寫才有說服力？有哪些可以量化或具體化的角度？",a:"<b>可用的角度：</b><ul><li><b>使用者面</b>：影響哪些角色、多少人、什麼頻率的操作</li><li><b>範圍面</b>：同樣修正套用到幾個頁面 / 模組</li><li><b>時間面</b>：減少了多少重工、查錯時間</li><li><b>流程面</b>：因此建立了什麼慣例</li></ul>「每天」「三個列表頁」這類<b>具體詞</b>比「提升了系統穩定性」有力。" },
  { m:4,w:2,d:1,t:"任務題",q:"用完整模板寫一個「畫面欄位與資料不一致」的 API / 資料流追蹤案例（背景到影響七段都要有）。",a:"<b>各段自我檢查：</b><ul><li>背景：系統 + 畫面 + 欄位名稱</li><li>問題：畫面顯示 X，但實際應該是 Y（兩個值都寫出來）</li><li>追查：有沒有寫出「我先看 response 對不對」的二分過程</li><li>原因：落點明確——送錯參數、後端資料、還是前端 mapping？</li><li>修正：具體改了哪個檔案 / 哪段邏輯</li><li>驗證：用哪幾筆資料、哪些情境比對</li><li>影響：這個欄位對使用者的決策有多重要</li></ul>" },
  { m:4,w:2,d:2,t:"任務題",q:"為你負責的一個頁面建立「API 對照表」。這張表應該有哪些欄位？",a:"<b>建議欄位：</b><pre>| 畫面欄位 | API           | response 路徑     | 型別   | 空值處理 |\n|----------|---------------|-------------------|--------|-----------|\n| 訂單編號 | GET /orders   | data.list[].no    | string | 不會為空  |\n| 出貨時間 | GET /orders   | data.list[].ship_at| string|null | 顯示「—」 |</pre>" },
  { m:4,w:2,d:3,t:"任務題",q:"用純文字（箭頭式）畫出你某個功能的資料流圖：從使用者操作到畫面更新。",a:"<b>格式示範：</b><pre>[使用者] 點擊「查詢」\n   ↓ onClick\n[SearchForm] 收集條件 → onSearch(query)\n   ↓\n[UserPage] handleSearch → setPage(1)\n   ↓\n[api/user.ts] getUsers({query, page})\n   ↓ GET /api/users?keyword=...\n[後端] 查 DB → 回 { list, total }\n   ↓\n[UserPage] setList / setTotal\n   ↓ re-render\n[UserTable] 顯示新資料</pre>" },
  { m:4,w:2,d:4,t:"任務題",q:"發現某支 API 的 response 疑似有問題，要去問後端。用「問問題模板」寫出一則完整的訊息。",a:"<b>示範：</b><pre>【訂單列表 ship_at 欄位疑似異常】\n背景：訂單管理系統列表頁，串 GET /api/orders\n現象：訂單編號 A1234 的 ship_at 回傳 null\n預期：這筆訂單在後台已標記出貨，應有時間\n已查：前端送出參數正確（截圖）；Network response 中該欄位即為 null（截圖）\n判斷：懷疑是該筆資料寫入時未更新 ship_at\n想確認：DB 中這筆的 ship_at 實際值？</pre>" },
  { m:4,w:2,d:5,t:"觀念題",q:"案例中「現象」和「原因」常被混在一起寫。它們的差別是什麼？怎麼自我檢查有沒有寫混？",a:"<ul><li><b>現象</b>：觀察到的事實——「畫面顯示金額為 0」「第 2 頁篩選後查無資料」</li><li><b>原因</b>：經過追查確認的根因——「mapping 時把 amount 寫成 ammount 拿到 undefined」</li></ul><b>自我檢查法：</b>原因句必須能回答「<b>為什麼</b>會出現那個現象」，而且是你<b>驗證過</b>的。" },
  { m:4,w:3,d:1,t:"任務題",q:"寫一個「測試機正常、正式機出問題」（或相反）的案例。這類案例的「原因」通常落在哪幾類？",a:"<b>環境差異的常見根因：</b><ul><li>環境變數 / API base URL 設定不同</li><li>資料差異：測試機資料乾淨、正式機有歷史髒資料</li><li>版本差異：前後端部署版本不同步</li><li>快取：CDN、瀏覽器快取了舊版 bundle</li><li>權限 / 防火牆 / CORS 設定不同</li></ul>" },
  { m:4,w:3,d:2,t:"任務題",q:"整理你參與過（或設想中）的災演 / 切換文件，它應該包含哪些章節？",a:"<b>災演文件基本結構：</b><ul><li><b>目的與範圍</b></li><li><b>前置檢查</b></li><li><b>操作步驟</b>（逐步，每步附預期結果）</li><li><b>驗證清單</b></li><li><b>回退程序</b></li><li><b>聯絡窗口與時程</b></li></ul>「整理災演操作文件，將切換步驟與驗證清單標準化，降低正式環境切換風險」——這正是計畫裡建議的履歷句之一。" },
  { m:4,w:3,d:3,t:"任務題",q:"為你的系統（或 CRUD Demo）寫一份「上線前檢查清單」，至少 8 項。",a:"<b>參考清單：</b><ul><li>☐ API base URL 指向正式環境</li><li>☐ console.log / debug 程式碼已移除</li><li>☐ 環境變數已確認</li><li>☐ 主要流程手動跑過：查詢、新增、編輯、刪除</li><li>☐ 空資料、錯誤狀態畫面正常</li><li>☐ 權限角色各登入測一輪</li><li>☐ build 產物在正式機可正常載入</li><li>☐ 回退方案確認</li><li>☐ 通知相關人員上線時間</li></ul>" },
  { m:4,w:3,d:4,t:"任務題",q:"設計一份「異常處理流程」文件的骨架：當系統出現問題時，團隊照著走的標準流程。",a:"<b>骨架：</b><pre>1. 接收與確認（誰回報、現象、影響範圍）\n2. 初步分級（P1 全面停擺 / P2 主要功能異常 / P3 局部問題）\n3. 排查（前端 Console / Network、後端 log、環境變更）\n4. 處置（能修就修；不能 → 回退或關閉功能開關）\n5. 記錄與覆盤（時間軸、根因、修正、預防措施）</pre>" },
  { m:4,w:3,d:5,t:"任務題",q:"把你 W3 整理的文件工作，濃縮成一句可放進履歷的描述。寫三個版本再挑最好的。",a:"<b>好句子的結構：</b>動詞開頭 + 具體產出 + 帶來的效果。<br><b>範例：</b><ul><li>「整理正式機 / 測試機切換與災演操作文件，將步驟與驗證清單標準化，降低環境切換風險」</li><li>「建立上線前檢查清單與異常處理流程，減少部署後緊急修復次數」</li></ul><b>避免：</b>「負責文件整理」這種沒有產出與效果的寫法。" },
  { m:4,w:4,d:1,t:"任務題",q:"把你最強的一個案例濃縮成 2 句履歷描述：第一句講你做了什麼，第二句講追查能力或成果。",a:"<b>示範：</b><br>「維護訂單管理系統列表與查詢功能，修正分頁與篩選條件互動造成的資料顯示異常。」<br>「從 UI 事件追蹤至 API 參數與 response 結構定位根因，並將修正擴及其他三個列表頁。」<br><b>檢查點：</b>有具體的系統 / 功能名詞；動詞是「修正、追蹤、定位」這類有力的詞。" },
  { m:4,w:4,d:2,t:"觀念題",q:"面試時要在 3–5 分鐘內講完一個案例。請設計你的時間配置與每段要講什麼。",a:"<b>建議配置（以 4 分鐘為例）：</b><ul><li><b>0:00–0:30 背景與問題</b>：系統、誰在用、出了什麼狀況</li><li><b>0:30–2:30 追查過程</b>（重頭戲）：起點 → 路徑 → 排除法 → 根因</li><li><b>2:30–3:15 修正與驗證</b></li><li><b>3:15–4:00 影響與學到的事</b></li></ul><b>練習法：</b>計時錄音講一次，聽回放檢查追查段有沒有佔到一半時間。" },
  { m:4,w:4,d:3,t:"觀念題",q:"面試官聽完你的案例後追問：「你怎麼確定根因就是那個，而不是別的？」要怎麼回答才站得住腳？",a:"<b>站得住腳的三層證據：</b><ol style='margin-left:22px'><li><b>重現</b>：照特定步驟操作，問題必然出現</li><li><b>機制</b>：能解釋從根因到現象的因果鏈</li><li><b>消失</b>：只改那一處，問題就不再重現；改回去，問題又出現</li></ol>回答模板：「我先找到穩定重現的步驟，接著驗證了因果機制，最後確認只修改該處問題就解除。」" },
  { m:4,w:4,d:4,t:"任務題",q:"檢視你的三個案例：每一個的「影響」段，找出至少一個可以加上的具體量詞或範圍詞。",a:"<b>可加的詞類型：</b><ul><li>頻率：「每日使用」「每月結算時」</li><li>範圍：「三個列表頁」「全部後台模組共用」</li><li>對象：「營運與客服共約 N 人」</li><li>前後對比：「原需手動比對半小時 → 修正後即時正確」</li></ul>" },
  { m:4,w:4,d:5,t:"任務題",q:"【本月驗收】三個案例完整度總檢查。",a:"<b>對照計畫第 4 個月驗收標準：</b><ul><li>☐ 3 個案例齊備：1 個畫面 / 表格、1 個 API / 資料流、1 個環境 / 文件流程</li><li>☐ 每個案例七段俱全</li><li>☐ 每個案例能在 3–5 分鐘內口述（實際計時過）</li><li>☐ 追查段講得出「思考順序與排除過程」</li><li>☐ 每個案例有 2 句履歷版描述</li></ul>" },

  /* ====== 第 5 個月：面試準備 ====== */
  { m:5,w:1,d:1,t:"觀念題",q:"【面試題】Promise 和 async / await 的差異是什麼？",a:"<b>參考回答：</b>「async/await 是 Promise 的<b>語法糖</b>，底層都是 Promise。差異在寫法：.then 用回呼串接；async/await 讓非同步程式長得像同步，搭配 try/catch 統一處理錯誤更直覺。實務上我預設用 async/await，但並行請求時會搭配 Promise.all，因為逐一 await 會變成序列執行。」<br><b>加分點：</b>主動講出 Promise.all 的並行差異。" },
  { m:5,w:1,d:2,t:"觀念題",q:"【面試題】map、filter、reduce 各適合什麼情境？請用實際工作例子回答。",a:"<b>參考回答：</b>「<b>map</b> 是一對一轉換——把 API 回傳的 snake_case 資料轉成畫面格式；<b>filter</b> 是篩選——表格前端篩出啟用中的帳號；<b>reduce</b> 是聚合成單一值——把訂單明細加總成總金額，或把陣列轉成以 id 為 key 的物件方便查找。三者都回傳新資料、不改原陣列，可以串接。」" },
  { m:5,w:1,d:3,t:"觀念題",q:"【面試題】深拷貝與淺拷貝的差異？實務上踩過什麼坑？",a:"<b>參考回答：</b>「淺拷貝只複製第一層，巢狀物件仍共用參考；深拷貝（structuredClone）整棵複製。我實際踩過的坑是<b>編輯 Modal</b>：把列表的 row 物件直接放進表單 state，使用者改了表單還沒按儲存，列表畫面就跟著變了。解法是帶入表單時複製一份。」" },
  { m:5,w:1,d:4,t:"觀念題",q:"【面試題】請說明 event loop 的基本概念。",a:"<b>參考回答：</b>「JS 是單執行緒，event loop 負責調度任務。同步程式先執行完；非同步完成的回呼依類型排隊：<b>微任務</b>（Promise.then）優先，每輪同步結束後先清空；<b>宏任務</b>（setTimeout、事件回呼）排在之後。所以 <code>setTimeout(fn, 0)</code> 一定晚於 Promise.then。」" },
  { m:5,w:1,d:5,t:"觀念題",q:"【面試題】什麼是閉包（closure）？舉一個你實際用到的例子。",a:"<b>參考回答：</b>「閉包是函式記得並能存取它定義時所在作用域的變數，即使在外層函式執行完之後。實際例子：debounce——內部函式持續記得外層的 timer 變數，每次輸入先清掉上一個計時器，達到停止輸入 300ms 才打 API。」<pre>function debounce(fn, delay) {\n  let timer;\n  return (...args) =&gt; {\n    clearTimeout(timer);\n    timer = setTimeout(() =&gt; fn(...args), delay);\n  };\n}</pre>" },
  { m:5,w:2,d:1,t:"觀念題",q:"【面試題】useState 和 useEffect 怎麼使用？各自的常見注意事項？",a:"<b>參考回答：</b>「useState 管理元件狀態，setter 觸發重渲染；注意 state 不可變（物件 / 陣列要建新的）、連續更新用函式式 <code>setX(prev => ...)</code>。useEffect 處理副作用（API、訂閱、DOM 操作），依賴陣列決定執行時機，用 cleanup 收拾計時器與監聽。」" },
  { m:5,w:2,d:2,t:"觀念題",q:"【面試題】useEffect 有哪些常見錯誤？你怎麼避免？",a:"<b>三個最常見：</b><ul><li><b>無限迴圈</b>：漏依賴陣列、或依賴了每次渲染都是新參考的物件 / 函式 → 用 useMemo / useCallback 穩定參考</li><li><b>沒寫 cleanup</b>：計時器、事件監聽殘留 → 養成「有訂閱就有退訂」的習慣</li><li><b>race condition</b>：舊請求晚回來覆蓋新資料 → 用 AbortController 取消</li></ul>" },
  { m:5,w:2,d:3,t:"觀念題",q:"【面試題】什麼是 props drilling？有哪些解法？",a:"<b>參考回答：</b>「props drilling 是資料要從頂層傳到深層元件，中間每一層都被迫經手自己用不到的 props。解法：<ul><li><b>元件組合</b>（children / slot）：先試這個</li><li><b>Context</b>：跨層共享的全域性資料</li><li><b>狀態管理庫</b>（Zustand / Redux）：多頁面共享時</li></ul>但兩三層的傳遞是正常的，不需要為此上工具。」" },
  { m:5,w:2,d:4,t:"觀念題",q:"【面試題】你怎麼決定何時拆分 component？拆分的依據是什麼？",a:"<b>參考回答：</b>「我的依據有三個：<b>職責</b>——一個元件能用一句話說清楚它負責什麼；<b>重用</b>——同樣的 UI 出現第二次就抽出來；<b>狀態邊界</b>——讓 state 住在最低的共同父層。反過來說，我不會為了拆而拆——過度拆分讓追蹤資料流變困難。」" },
  { m:5,w:2,d:5,t:"觀念題",q:"【面試題】如何避免不必要的 render？表單狀態又該怎麼管理？",a:"<b>避免不必要 render：</b>「先確認真的有效能問題（React DevTools Profiler），再依序：把 state 下放到最小範圍、React.memo 包純展示元件、useMemo / useCallback 穩定 props 參考。」<b>表單狀態：</b>「欄位少用一個物件 state + 通用 handleChange；欄位多、驗證複雜用 react-hook-form。」" },
  { m:5,w:3,d:1,t:"觀念題",q:"【面試題】type 和 interface 的差異？你的團隊（或你自己）的使用慣例是什麼？",a:"<b>參考回答：</b>「功能重疊度很高，差異：interface 可同名合併；type 能表達 union、交集、條件型別。我的慣例：<b>物件結構用 interface，狀態與組合型別用 type</b>，重點是專案內一致。被追問時補一句：union type 只能用 type 定義，這是最常遇到的硬差異。」" },
  { m:5,w:3,d:2,t:"觀念題",q:"【面試題】union type 在實務上怎麼用？請舉兩個例子。",a:"<b>參考回答：</b><ul><li><b>狀態列舉</b>：<code>type Status = 'idle' | 'loading' | 'success' | 'error'</code>，搭配 switch 渲染</li><li><b>可辨識聯合</b>：Modal state <code>{ open: false } | { open: true; mode: 'edit'; row: User }</code>，從型別上排除「編輯卻沒資料」的不可能狀態</li></ul>" },
  { m:5,w:3,d:3,t:"觀念題",q:"【面試題】請說明 generic 的基本概念，以及你在專案中怎麼用。",a:"<b>參考回答：</b>「generic 是<b>型別的參數化</b>，把會變動的型別抽成參數，讓一份定義服務多種型別且保持型別安全。我最常用在：<ul><li>API 共用外層：<code>ApiResponse&lt;T&gt;</code></li><li>共用請求函式：<code>request&lt;T&gt;(url): Promise&lt;T&gt;</code></li></ul>」" },
  { m:5,w:3,d:4,t:"觀念題",q:"【面試題】API response 的型別你會怎麼設計？欄位可能為 null 怎麼處理？",a:"<b>參考回答：</b>「我會照後端 response 由內往外逐層定義 interface，共用外層用 generic。可能為空的欄位誠實標 <code>string | null</code>，逼自己在畫面層處理空值。另外我習慣把後端 DTO 與前端 model 分開，在 service 層做 mapping。」" },
  { m:5,w:3,d:5,t:"觀念題",q:"【面試題】React props 的型別怎麼設計？事件、children、可選值各怎麼寫？",a:"<b>參考回答骨架：</b><pre>interface CardProps {\n  title: string;\n  count?: number;              // 可選 + 解構給預設值\n  onSelect: (id: number) =&gt; void; // 事件\n  children?: React.ReactNode;  // children 標準型別\n}</pre>補充：「props 是元件的對外合約，我會讓型別表達使用限制——例如狀態用 union 限定取值。」" },
  { m:5,w:4,d:1,t:"觀念題",q:"【面試題】如何從畫面追到 API？請完整描述你的流程。",a:"<b>回答：</b>「先確認現象發生在哪個欄位 / 操作 → 開 Network 找到對應請求 → 先看 <b>response 對不對</b>做二分：response 錯，檢查送出的 payload / query，參數對就找後端；response 對，問題在前端 mapping 與渲染邏輯，必要時下斷點。最後說清楚問題落在『參數、後端資料、前端轉換』哪一段。」<br><b>最後加：</b>「我也習慣整理畫面欄位與 API 的對照表。」" },
  { m:5,w:4,d:2,t:"觀念題",q:"【面試題】表格分頁和查詢條件你會怎麼處理？前後端怎麼配合？",a:"<b>參考回答：</b>「分頁用後端分頁：前端送 page / pageSize，後端回 list / total。設計上注意：<b>條件或 pageSize 變更時 page 重設為 1</b>；每次請求都帶完整條件 + 頁碼；條件同步到網址 query string 讓重整可還原。串接前先跟後端對齊 page 從 0 還是 1 開始。」" },
  { m:5,w:4,d:3,t:"觀念題",q:"【面試題】如何 debug 線上（正式環境）問題？和開發環境有什麼不同？",a:"<b>參考回答：</b>「線上不能隨意改 code，流程是：先確認影響範圍與重現步驟 → 看 Console 與 Network 蒐集證據 → 比對最近的部署與設定變更 → 檢查環境差異 → 嚴重時先止血（回退版本）→ 事後在開發環境重現並補防護。」<b>關鍵詞：</b>「先止血再根治」「環境差異清單」。" },
  { m:5,w:4,d:4,t:"觀念題",q:"【面試題】改完一個功能，如何確認沒有影響到其他功能？",a:"<b>參考回答：</b>「三層確認：<b>影響面分析</b>——搜尋被改的函式 / 元件被誰引用，列出受影響清單；<b>測試</b>——跑原 bug 的重現路徑 + 相鄰功能回歸；<b>小步發布</b>——commit 拆小、PR 說明寫清楚改動範圍。共用元件的修改我會特別保守。」" },
  { m:5,w:4,d:5,t:"任務題",q:"【本週任務】完成 1 分鐘自我介紹+本月總驗收。",a:"<b>自我介紹（計時練到 60 秒內）：</b>經驗主線（React / Vue / TS / 後台系統）→ 做過什麼 → 差異化能力（資料流追蹤）→ 下一步期望。<br><b>第 5 個月驗收：</b><ul><li>☐ 新版履歷完成</li><li>☐ 自我介紹 60 秒內講順</li><li>☐ 三個案例稿能口述</li><li>☐ 本月 20 題面試題每題能不看答案回答</li><li>☐ GitHub Demo + README 整理完畢</li></ul>" },

  /* ====== 第 6 個月：投遞與修正 ====== */
  { m:6,w:1,d:1,t:"任務題",q:"打開人力銀行，用計畫裡的關鍵字搜出 10 個職缺。練習判斷：什麼叫「符合 60–70% 就可以投」？",a:"<b>判斷法：把 JD 的要求分三類：</b><ul><li><b>核心要求</b>（React / Vue、JS、API 串接）：你要會</li><li><b>加分要求</b>（Next.js、測試、CI/CD）：缺幾個沒關係</li><li><b>裝飾要求</b>（「熟悉各種前端技術」）：忽略</li></ul><b>結論：</b>核心符合 + 加分缺一些 = 投。JD 是廠商的「願望清單」，不是門檻。" },
  { m:6,w:1,d:2,t:"任務題",q:"挑一個你想投的職缺，針對它的 JD 客製你的履歷。要調整哪三個地方？",a:"<b>三處客製：</b><ul><li><b>技能排序</b>：JD 主打 Vue 就把 Vue 往前放</li><li><b>專案描述用詞</b>：對方 JD 出現的詞借來用</li><li><b>案例選擇</b>：金融業 → 放災演 / 文件案例；新創 → 放快速開發案例</li></ul><b>原則：</b>不是造假，是<b>調整聚光燈</b>。" },
  { m:6,w:1,d:3,t:"任務題",q:"建立你的投遞紀錄表。應該有哪些欄位？",a:"<b>建議欄位：</b><pre>| 公司 | 職缺 | 投遞日 | 來源 | 核心符合度 | 狀態 | 回應日 | 面試日 | 結果 | 備註 |</pre><b>狀態值：</b>已投 / 已讀 / 邀約 / 一面 / 二面 / offer / 婉拒 / 無回應。<b>為什麼重要：</b>回應率本身是訊號——投 20 個全無回應 = 履歷有問題；有面試但過不了 = 面試技巧或能力缺口。" },
  { m:6,w:1,d:4,t:"任務題",q:"對著鏡子或錄音，計時做一次 1 分鐘自我介紹。用這份標準幫自己評分。",a:"<b>評分標準（每項 0–2 分，滿分 10）：</b><ul><li>時間控制：55–70 秒內結束</li><li>主線清楚：聽完知道你是「做後台系統的前端、強在資料流追蹤」</li><li>有具體詞：說出系統類型、技術棧</li><li>差異化：有講出別人少有的點</li><li>結尾有方向：說出想成為什麼、為何投這家</li></ul><b>低於 7 分：</b>修稿再錄一次。" },
  { m:6,w:1,d:5,t:"任務題",q:"寫一段應徵信開頭，150 字以內，讓人資想往下看。",a:"<b>結構：你是誰一句 + 你和這職缺的交集一句 + 證據一句。</b><br><b>範例：</b>「您好，我是有後台管理系統開發經驗的前端工程師，主力 React / TypeScript。看到貴公司職缺重視資料密集的儀表板開發，與我日常負責的查詢、表格、API 串接工作高度相符。我習慣從 UI 事件追蹤至 API 與資料來源定位問題，相關作品整理於 GitHub，期待有機會進一步說明。」" },
  { m:6,w:2,d:1,t:"任務題",q:"收到面試邀約了。列出你「面試前一天」的準備清單。",a:"<b>前一天清單：</b><ul><li>☐ 重讀 JD，預測會問的方向</li><li>☐ 查公司產品：實際打開他們的網站 / App 用 10 分鐘</li><li>☐ 複習自我介紹 + 三個案例（口述各一次）</li><li>☐ 掃過第 5 個月的 20 題面試題</li><li>☐ 準備 3 個反問問題</li><li>☐ 確認時間地點 / 視訊連結、測試鏡頭麥克風</li><li>☐ 作品集連結、履歷先開好</li></ul>" },
  { m:6,w:2,d:2,t:"觀念題",q:"「你有什麼問題想問我們嗎？」——準備 3 個好的反問問題，並說明什麼問題不要問。",a:"<b>好問題：</b><ul><li>「前端團隊目前的技術棧與分工方式？新人前三個月通常負責什麼？」</li><li>「團隊目前最想解決的前端技術挑戰是什麼？」</li><li>「程式碼有 code review 流程嗎？怎麼進行？」</li></ul><b>第一輪先別問：</b>薪資福利細節。<b>千萬別說：</b>「沒有問題」——等於說你沒做功課。" },
  { m:6,w:2,d:3,t:"觀念題",q:"遇到現場寫題（白板題 / live coding），正確的答題流程是什麼？",a:"<b>五步流程：</b><ol style='margin-left:22px'><li><b>確認需求</b>：複述題目、問清邊界</li><li><b>講思路</b>：先說打算怎麼解</li><li><b>先求有再求好</b>：先寫出暴力解</li><li><b>自己測</b>：拿正常 + 邊界輸入走一遍</li><li><b>主動講優化</b>：時間複雜度、可讀性</li></ol>面試官評的是<b>過程與溝通</b>，全程出聲思考。" },
  { m:6,w:2,d:4,t:"觀念題",q:"被問到不會的題目，怎麼回答最不扣分（甚至加分）？",a:"<b>三段式回答：</b><ol style='margin-left:22px'><li><b>誠實但不躺平</b>：「這個我沒有實際用過」</li><li><b>連結已知</b>：「不過聽起來和我用過的 ___ 在解類似問題…」</li><li><b>展現學習習慣</b>：「如果工作需要，我會先看官方文件做個小 demo 驗證」</li></ol>面試官在測的是你<b>遇到不會的東西時的反應</b>。" },
  { m:6,w:2,d:5,t:"觀念題",q:"被問「期望薪資多少」，怎麼回答？事前要做什麼功課？",a:"<b>事前功課：</b>查同職級行情，算出理想 / 滿意 / 底線。<b>回答策略：</b><ul><li>給<b>區間</b>：「期望月薪 70K–85K，依職責範圍與整體福利可以討論」</li><li>被壓價時回到價值：「我能獨立負責後台模組，希望薪資能反映這個能力」</li></ul><b>心法：</b>你是在<b>談</b>薪資，不是被動接受。" },
  { m:6,w:3,d:1,t:"任務題",q:"剛結束一場面試（或模擬面試）。趁記憶猶新，用檢討格式完整記錄一次。",a:"<b>照計畫的格式逐項填：</b><pre>公司名稱：\n職缺名稱：\n面試日期：\n被問到的技術題：\n回答不好的地方：\n我不會的地方：\n下次要補強的內容：\n這間公司要求的能力：\n是否繼續投類似職缺：</pre><b>黃金時間：</b>面試結束 30 分鐘內記錄，過一晚會忘掉一半題目。" },
  { m:6,w:3,d:2,t:"觀念題",q:"怎麼把「面試被問倒的題目」變成你題庫的一部分？設計一個固定流程。",a:"<b>四步流程：</b><ol style='margin-left:22px'><li><b>收錄</b>：把題目原文寫進面試題庫</li><li><b>研究</b>：查資料寫出 5–8 行的參考答案，附程式例子</li><li><b>連結</b>：想一個自己工作 / Demo 中的對應實例</li><li><b>複誦</b>：三天後不看答案口述一次</li></ol>被問倒的題目是<b>市場直接告訴你的考試範圍</b>，比任何題庫都精準。" },
  { m:6,w:3,d:3,t:"任務題",q:"檢視你目前的技能缺口清單，用什麼標準排優先序？排出你的前三名。",a:"<b>排序公式：出現頻率 × 補強成本。</b><ul><li><b>高頻 + 低成本</b> → 第一優先（多場面試都問、一週能補）</li><li><b>高頻 + 高成本</b> → 排入下個月主題，每週固定推進</li><li><b>低頻 + 高成本</b> → 除非目標職缺明確要求，否則先放著</li></ul><b>判斷頻率的依據：</b>你的面試檢討紀錄——被問兩次以上就是高頻。" },
  { m:6,w:3,d:4,t:"觀念題",q:"什麼訊號告訴你「該改履歷第二版了」？各種訊號分別對應什麼修改？",a:"<b>訊號 → 對策：</b><ul><li><b>投 15–20 個無回應</b> → 履歷第一屏問題 → 改技能區與摘要</li><li><b>有面試但常被問「你真的做過 X 嗎」</b> → 某段描述寫過頭 → 改成可被追問的具體寫法</li><li><b>面試官總是只聊到某一個專案</b> → 那段最吸引人 → 把同類經驗放大</li><li><b>邀約的職缺類型不是你想要的</b> → 履歷散發的訊號錯了 → 重新對齊目標職缺用詞</li></ul>" },
  { m:6,w:3,d:5,t:"任務題",q:"寫兩封信的模板：(1) 面試後一週無消息的跟進信 (2) 婉拒 offer 但保持關係的信。",a:"<b>(1) 跟進信：</b>「您好，我是 X 月 X 日面試前端工程師職位的＿＿＿。想跟進目前的甄選進度，若需要補充作品或資訊請隨時告訴我。謝謝您！」——短、禮貌、不卑不亢。<br><br><b>(2) 婉拒信：</b>「非常感謝貴公司的肯定與 offer。經過審慎考量，我決定接受另一個更符合目前職涯規劃的機會。面試過程中與團隊的交流讓我收穫很多，希望未來有機會合作。」" },
  { m:6,w:4,d:1,t:"任務題",q:"設計你的每週投遞節奏：5–10 個職缺怎麼分配到一週裡？搭配原本的學習節奏怎麼調整？",a:"<b>建議節奏：</b><ul><li><b>週日</b>：搜尋 + 篩選本週要投的職缺（30 分）</li><li><b>週一、三</b>：各投 2–3 個，每個花 10 分鐘做履歷微客製</li><li><b>週二、四</b>：原本的實作練習改為「補強面試檢討出的缺口」</li><li><b>週五</b>：更新紀錄表、整理本週面試檢討</li></ul>" },
  { m:6,w:4,d:2,t:"任務題",q:"把你累積的面試題整理成一份「個人面試題庫」。設計它的結構。",a:"<b>建議結構：</b><pre>分類：JS / React / TS / 實務 / 人格特質\n每題欄位：\n- 題目原文\n- 被問的公司與次數\n- 我的參考答案（5–8 行）\n- 對應的個人實例\n- 掌握度：✅ 能流暢回答 / 🔶 需要看稿 / ❌ 還不會</pre><b>使用方式：</b>每次面試前掃 🔶 和 ❌；每次面試後新增與更新次數。" },
  { m:6,w:4,d:3,t:"觀念題",q:"假設你同時拿到兩個 offer，要用哪些維度比較？只比薪水會漏掉什麼？",a:"<b>比較維度：</b><ul><li><b>薪酬全貌</b>：月薪 × 保障月數 + 獎金 + 加班制度</li><li><b>技術成長</b>：技術棧新舊、有沒有 code review</li><li><b>職責範圍</b>：是「獨立負責模組」還是「只切版改畫面」</li><li><b>團隊與主管</b>：工程文化</li><li><b>履歷價值</b>：兩年後這段經歷讓你的下一步更值錢嗎？</li></ul>" },
  { m:6,w:4,d:4,t:"任務題",q:"練習談薪：寫出三句你真的講得出口的談薪語句（開價、被壓價回應、爭取空間）。",a:"<b>範例：</b><ul><li><b>開價</b>：「我的期望是月薪 75K 到 85K，這是參考我能獨立負責後台模組的能力與市場行情。」</li><li><b>被壓價</b>：「我理解預算有考量。如果月薪到不了這個區間，想了解獎金、調薪機制或職級上有沒有空間？」</li><li><b>爭取</b>：「如果能到 80K，我可以這週就給答覆。」</li></ul><b>心法：</b>開完價就停，不要急著自己降價。" },
  { m:6,w:4,d:5,t:"任務題",q:"【六個月總驗收】對照整份計畫，做最終檢查。",a:"<b>最終清單：</b><ul><li>☐ JS / TS / React 主線達到檢查表目標分數</li><li>☐ 一個完整 CRUD 後台 Demo + README 在 GitHub 上</li><li>☐ 3 個七段俱全、能口述的工作案例</li><li>☐ 新版履歷（含案例化描述）+ 客製化流程</li><li>☐ 60 秒自我介紹 + 個人面試題庫</li><li>☐ 投遞紀錄表運轉中、面試檢討有累積</li><li>☐ 我知道市場在問什麼、我卡在哪、下一步補什麼</li></ul><b>然後回頭看那句目標：</b>「從會改畫面的人，變成能獨立負責後台模組、能從 UI 追到 API 與資料來源的前端工程師。」🌊" },
];

export const MONTH_NAMES = {
  1: "第 1 個月・JS / TS 基礎",
  2: "第 2 個月・React 主線",
  3: "第 3 個月・API 與資料流",
  4: "第 4 個月・工作案例",
  5: "第 5 個月・面試準備",
  6: "第 6 個月・投遞與修正",
};

export const DAY_NAMES = ["", "週一", "週二", "週三", "週四", "週五"];
